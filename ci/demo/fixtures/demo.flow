# This is a placeholder for the hand-crafted mitmproxy flow file.
# In production, this file is a binary mitmproxy flow dump (.flow format).
# 
# To generate a real demo.flow:
#   1. Start mitmproxy: mitmdump -w ci/demo/fixtures/demo.flow
#   2. Configure your browser/app to use proxy http://localhost:8080
#   3. Browse http://demo-app:3000 to capture traffic
#   4. Stop mitmdump (Ctrl+C)
#
# The flow file captures HTTP traffic to http://demo-app:3000
# which is used by phase2.tape to demonstrate mitm2openapi.
#
# For CI, this file should be a real binary .flow file committed to the repo.
# Replace this placeholder with an actual captured flow.
